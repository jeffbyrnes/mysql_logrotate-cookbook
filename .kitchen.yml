---
driver:
  name: vagrant
  customize:
    memory: 4096

provisioner:
  name: chef_zero

verifier:
  name: inspec

platforms:
  - name: ubuntu-16.04

suites:
  - name: default
    run_list:
      - recipe[mysql_logrotate_test::default]
    attributes:
